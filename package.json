{
  "name": "giwa-hardhat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:giwa": "hardhat run scripts/deploy.js --network giwa",
    "verify:giwa": "hardhat verify --network giwa"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.26.3"
  }
}
